version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd web-app && npm ci
    build:
      commands:
        - cd web-app && npm run build
  artifacts:
    baseDirectory: web-app/.next
    files:
      - '**/*'
  cache:
    paths:
      - web-app/.next/cache/**/*
      - web-app/node_modules/**/*
